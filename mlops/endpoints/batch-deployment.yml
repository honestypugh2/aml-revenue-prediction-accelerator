# Pipeline-component deployment avoids the incompatible dependency overlay
# injected by Azure ML's no-code model batch runtime.
$schema: https://azuremlschemas.azureedge.net/latest/pipelineComponentBatchDeployment.schema.json
type: pipeline
name: champion-pipeline
endpoint_name: revenue-batch-endpoint
description: Champion net-revenue regression model (batch scoring).
component: ../components/batch_scoring_pipeline.yml
